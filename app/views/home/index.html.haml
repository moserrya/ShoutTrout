- content_for :head do
  = tag :meta, name: 'stripe-key', content: STRIPE_PUBLIC_KEY

%nav.top-bar{data: {topbar: ''}}
  %ul.title-area
    %li.name
      %h1
        %a{href: '/'} ShoutTrout
  %section.top-bar-section
    %ul.right
      %li
        %a{href: '/about'} About
      %li
        %a{href: '/login'} Log In


- content_for :javascripts do
  = javascript_include_tag "stripe.v2.min.js"
